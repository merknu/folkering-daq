[package]
name = "peak-detector"
version = "0.1.0"
edition = "2021"
description = "Peak detector with configurable threshold for Folkering DAQ"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
lto = true
strip = true
panic = "abort"
codegen-units = 1
